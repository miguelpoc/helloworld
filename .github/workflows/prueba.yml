name: Hello World example
on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar mensaje
        run: |
          echo "Hola mundo!"
      - name: Mostrar fecha
        run: date
      - run: ls -l
